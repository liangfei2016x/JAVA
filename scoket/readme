UDP实现步骤：

     服务器端：

     1，创建DatagramSocket，指定端口号。

     2，创建DatagramPacket。

     3，接受客户端发送的数据信息。

     4，读取客户端数据。

     5，关闭资源。

     客户端：

     1，定义服务器的地址，端口号，数据，发送信息。

     2，创建DatagramPacket，包含将要发送的信息。

     3，创建DatagramSocket。

     4，向服务器端发送数据。

     5，关闭资源。